apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: eshope
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: eshope-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota
  namespace: ${TENANT}
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "4"
    limits.memory: 8Gi
    pods: "10"
